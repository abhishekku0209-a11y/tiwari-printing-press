[build]
  base = "src/frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing: redirect all paths to index.html so React Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
